# Copy to deploy/.env. Compose loads this into the container.
# Use ONE of: an Anthropic API key, or a logged-in ~/.claude (mounted volume).
ANTHROPIC_API_KEY=

# Optional overrides:
# PROJECT_PATH=/abs/path/to/your/project   # mounted at /workspace
# AIFLEET_DAEMON_URL=http://localhost:7878
